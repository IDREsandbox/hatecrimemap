import React from 'react';

import ReportIncidentPage from './ReportIncidentPage';

describe('ReportIncidentPage', () => {
  it('renders without crashing', () => {
    mount(<ReportIncidentPage />);
  });
});
